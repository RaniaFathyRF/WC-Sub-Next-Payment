<?php
/**
 * Define plugin url.
 */
if ( ! defined( 'WCS_NEXT_PAYMENT_URL' ) ) {
	define( 'WCS_NEXT_PAYMENT_URL', plugin_dir_url( __FILE__ ) );
}

/**
 * Define plugin path.
 */
if ( ! defined( 'WCS_NEXT_PAYMENT_PATH' ) ) {
	define( 'WCS_NEXT_PAYMENT_PATH', plugin_dir_path( __FILE__ ) );
}

/**
 * Define plugin assests version.
 */
if ( ! defined( 'WCS_NEXT_PAYMENT_ASSETS_VERSION' ) ) {
	define( 'WCS_NEXT_PAYMENT_ASSETS_VERSION', '1.0.0' );
}


